# محاسبات اعشاری (Floating Point) در RISC-V

<div dir="rtl">

## 📑 فهرست مطالب

- [مقدمه](#مقدمه)
- [نماد علمی](#نماد-علمی)
- [استاندارد IEEE 754](#استاندارد-ieee-754)
- [نمایش دودویی](#نمایش-دودویی)
- [دستورات اعشاری در RISC-V](#دستورات-اعشاری-در-risc-v)
- [رجیسترهای اعشاری](#رجیسترهای-اعشاری)
- [عملیات پایه](#عملیات-پایه)
- [تبدیلات](#تبدیلات)
- [مقایسه](#مقایسه)
- [استثناها و خطاها](#استثناها-و-خطاها)
- [مثال‌های عملی](#مثال‌های-عملی)

---

## مقدمه

### 🎯 اعداد حقیقی (Real Numbers)

در برنامه‌نویسی، علاوه بر اعداد صحیح، به اعدادی نیاز داریم که شامل **کسر** باشند.

#### مثال‌های کاربردی:

<div dir="ltr">

```
🥧 π = 3.14159265...
📐 e = 2.71828...
⏱️ 1 نانوثانیه = 0.000000001 = 1.0 × 10⁻⁹
📅 1 قرن = 3,155,760,000 ثانیه = 3.15576 × 10⁹
```

</div>

### 💡 چرا Floating Point؟

| نیاز | مثال | راه‌حل |
|------|------|--------|
| **کسرهای کوچک** | <div dir="ltr">`0.0001`</div> | ❌ عدد صحیح کافی نیست |
| **اعداد بسیار بزرگ** | <div dir="ltr">`10²³`</div> | ❌ خارج از محدوده int |
| **دقت علمی** | <div dir="ltr">`3.14159...`</div> | ✅ Floating Point |
| **محدوده وسیع** | <div dir="ltr">`10⁻³⁰⁸ تا 10³⁰⁸`</div> | ✅ Floating Point |

---

## نماد علمی

### 📐 Scientific Notation

اعداد را به فرم زیر می‌نویسیم:

<div dir="ltr">

```
±d.ddd... × 10ⁿ
```

</div>

**اجزا:**
- <span dir="ltr">**d**</span>: رقم (0-9)
- <span dir="ltr">**n**</span>: توان (exponent)

### 🎓 نرمال‌سازی (Normalization)

**عدد نرمال‌شده:** عددی که یک رقم غیرصفر در سمت چپ نقطه اعشار دارد.

#### مثال‌ها:

<div dir="ltr">

```
✅ نرمال شده:
   1.0 × 10⁻⁹
   3.14159 × 10⁰
   6.022 × 10²³

❌ نرمال نشده:
   0.1 × 10⁻⁸     → 1.0 × 10⁻⁹
   31.4159 × 10⁻¹ → 3.14159 × 10⁰
   0.06022 × 10²⁵ → 6.022 × 10²³
```

</div>

### 📊 مزایای نرمال‌سازی

```
┌─────────────────────────────────────┐
│  ✅ تبادل داده ساده‌تر            │
│  ✅ الگوریتم‌های محاسباتی ساده‌تر │
│  ✅ دقت بیشتر                      │
│  ✅ یکتایی نمایش                   │
└─────────────────────────────────────┘
```

---

## استاندارد IEEE 754

### 🌐 استاندارد جهانی

**IEEE 754** استاندارد بین‌المللی برای نمایش اعداد اعشاری است.

### 📦 فرمت‌های اصلی

#### 1️⃣ Single Precision (32-bit)

<div dir="ltr">

```
┌──┬────────┬───────────────────────┐
│S │Exponent│      Fraction         │
│1 │  8 bit │       23 bit          │
└──┴────────┴───────────────────────┘
 31   30-23        22-0

کل: 32 بیت
```

</div>

#### 2️⃣ Double Precision (64-bit)

<div dir="ltr">

```
┌──┬───────────┬──────────────────────────────────┐
│S │ Exponent  │          Fraction                │
│1 │  11 bit   │          52 bit                  │
└──┴───────────┴──────────────────────────────────┘
 63   62-52              51-0

کل: 64 بیت
```

</div>

### 🔍 اجزای فرمت

<div dir="ltr">

| جزء | نام | بیت‌ها (SP) | بیت‌ها (DP) | توضیح |
|-----|-----|------------|-------------|-------|
| **S** | Sign | 1 | 1 | علامت: 0=مثبت, 1=منفی |
| **E** | Exponent | 8 | 11 | توان با bias |
| **F** | Fraction | 23 | 52 | کسر (mantissa) |

</div>

### 🧮 فرمول محاسبه

**عدد واقعی:**

<div dir="ltr">

```
(-1)ˢ × (1.F) × 2^(E-Bias)

جایی که:
  S = بیت علامت
  F = بخش کسری
  E = نما (Exponent)
  Bias = 127 (single) یا 1023 (double)
```

</div>

---

## نمایش دودویی

### 🔢 تبدیل به دودویی

#### مثال 1: عدد 5.75

<div dir="ltr">

```
قسمت صحیح:   5₁₀ = 101₂
قسمت کسری: 0.75₁₀ = 0.11₂

نتیجه: 5.75₁₀ = 101.11₂
```

</div>

**مراحل تبدیل کسری:**

<div dir="ltr">

```
0.75 × 2 = 1.5  → بیت 1
0.5  × 2 = 1.0  → بیت 1
0.0  × 2 = 0.0  → پایان

نتیجه: 0.11₂
```

</div>

#### نرمال‌سازی:

<div dir="ltr">

```
101.11₂ = 1.0111 × 2³

اجزا:
  Sign: 0 (مثبت)
  Exponent: 3 + 127 = 130 = 10000010₂
  Fraction: 0111 00000000000000000000
```

</div>

**نمایش کامل 32 بیتی:**

<div dir="ltr">

```
┌─┬────────┬───────────────────────┐
│0│10000010│01110000000000000000000│
└─┴────────┴───────────────────────┘
```

</div>

### 📊 جدول مثال‌ها

<div dir="ltr">

| اعشاری | دودویی | نرمال شده | Sign | Exp | Fraction |
|--------|--------|-----------|------|-----|----------|
| 1.0 | 1.0 | 1.0 × 2⁰ | 0 | 127 | 000...0 |
| -2.0 | -10.0 | -1.0 × 2¹ | 1 | 128 | 000...0 |
| 0.5 | 0.1 | 1.0 × 2⁻¹ | 0 | 126 | 000...0 |
| 3.5 | 11.1 | 1.11 × 2¹ | 0 | 128 | 110...0 |

</div>

### 🎯 محدوده‌های قابل نمایش

#### Single Precision (32-bit):

<div dir="ltr">

```
بزرگترین:  ≈ 3.4 × 10³⁸
کوچکترین: ≈ 1.2 × 10⁻³⁸

دقت: ~7 رقم اعشار
```

</div>

#### Double Precision (64-bit):

<div dir="ltr">

```
بزرگترین:  ≈ 1.8 × 10³⁰⁸
کوچکترین: ≈ 2.2 × 10⁻³⁰⁸

دقت: ~16 رقم اعشار
```

</div>

---

## دستورات اعشاری در RISC-V

### 📋 توسعه F و D

**RISC-V Extensions:**
- **F**: Single Precision (32-bit)
- **D**: Double Precision (64-bit)

### 🎛️ دستورات اصلی

#### عملیات حسابی

<div dir="ltr">

```assembly
# Single Precision (F)
fadd.s  fd, fs1, fs2    # fd = fs1 + fs2
fsub.s  fd, fs1, fs2    # fd = fs1 - fs2
fmul.s  fd, fs1, fs2    # fd = fs1 × fs2
fdiv.s  fd, fs1, fs2    # fd = fs1 ÷ fs2

# Double Precision (D)
fadd.d  fd, fs1, fs2    # جمع double
fsub.d  fd, fs1, fs2    # تفریق double
fmul.d  fd, fs1, fs2    # ضرب double
fdiv.d  fd, fs1, fs2    # تقسیم double
```

</div>

#### دستورات پیشرفته

<div dir="ltr">

```assembly
# جذر
fsqrt.s fd, fs1         # fd = √fs1
fsqrt.d fd, fs1

# حداقل و حداکثر
fmin.s  fd, fs1, fs2    # fd = min(fs1, fs2)
fmax.s  fd, fs1, fs2    # fd = max(fs1, fs2)

# علامت
fsgnj.s  fd, fs1, fs2   # fd = |fs1| × sign(fs2)
fsgnjn.s fd, fs1, fs2   # fd = |fs1| × -sign(fs2)
fsgnjx.s fd, fs1, fs2   # fd = |fs1| × (sign(fs1) ⊕ sign(fs2))

# قدر مطلق و نقیض
fabs.s  fd, fs1         # fd = |fs1| (شبه‌دستور)
fneg.s  fd, fs1         # fd = -fs1 (شبه‌دستور)
```

</div>

---

## رجیسترهای اعشاری

### 📝 رجیسترهای F

**RISC-V دارای 32 رجیستر اعشاری است:**

<div dir="ltr">

```
f0-f31: رجیسترهای اعشاری
```

</div>

#### کنوانسیون استفاده:

<div dir="ltr">

| رجیستر | نام ABI | کاربرد | حفظ توسط |
|---------|---------|--------|-----------|
| **f0-f7** | ft0-ft7 | موقت | Caller |
| **f8-f9** | fs0-fs1 | ذخیره‌شده | Callee |
| **f10-f17** | fa0-fa7 | آرگومان/نتیجه | Caller |
| **f18-f27** | fs2-fs11 | ذخیره‌شده | Callee |
| **f28-f31** | ft8-ft11 | موقت | Caller |

</div>

### 🔄 نقل و انتقال داده

<div dir="ltr">

```assembly
# بارگذاری از حافظه
flw  ft0, 0(a0)         # بارگذاری float
fld  ft0, 0(a0)         # بارگذاری double

# ذخیره در حافظه
fsw  ft0, 0(a0)         # ذخیره float
fsd  ft0, 0(a0)         # ذخیره double

# کپی بین رجیسترها
fmv.s fd, fs1           # fd = fs1 (single)
fmv.d fd, fs1           # fd = fs1 (double)
```

</div>

---

## عملیات پایه

### ➕ مثال: جمع دو عدد

<div dir="ltr">

```assembly
# محاسبه: result = a + b

main:
    # بارگذاری مقادیر
    la   t0, value_a
    flw  fa0, 0(t0)         # fa0 = a
    
    la   t0, value_b
    flw  fa1, 0(t0)         # fa1 = b
    
    # جمع
    fadd.s fa2, fa0, fa1    # fa2 = a + b
    
    # ذخیره نتیجه
    la   t0, result
    fsw  fa2, 0(t0)

.data
value_a: .float 3.14
value_b: .float 2.71
result:  .float 0.0
```

</div>

### ✖️ مثال: محاسبه مساحت دایره

<div dir="ltr">

```assembly
# محاسبه: area = π × r²

circle_area:
    # ورودی: fa0 = radius
    # خروجی: fa0 = area
    
    # محاسبه r²
    fmul.s ft0, fa0, fa0    # ft0 = r × r
    
    # بارگذاری π
    la   t0, pi
    flw  ft1, 0(t0)         # ft1 = π
    
    # محاسبه π × r²
    fmul.s fa0, ft1, ft0    # fa0 = π × r²
    
    ret

.data
pi: .float 3.14159265
```

</div>

---

## تبدیلات

### 🔄 تبدیل بین انواع

<div dir="ltr">

```assembly
# Integer ← Float
fcvt.w.s  rd, fs1       # int32 = float
fcvt.wu.s rd, fs1       # uint32 = float
fcvt.l.s  rd, fs1       # int64 = float
fcvt.lu.s rd, fs1       # uint64 = float

# Float ← Integer
fcvt.s.w  fd, rs1       # float = int32
fcvt.s.wu fd, rs1       # float = uint32
fcvt.s.l  fd, rs1       # float = int64
fcvt.s.lu fd, rs1       # float = uint64

# Double ← Float
fcvt.d.s  fd, fs1       # double = float

# Float ← Double
fcvt.s.d  fd, fs1       # float = double
```

</div>

### 📐 مثال: تبدیل دما

<div dir="ltr">

```assembly
# تبدیل Celsius به Fahrenheit
# F = C × 9/5 + 32

celsius_to_fahrenheit:
    # ورودی: fa0 = celsius
    # خروجی: fa0 = fahrenheit
    
    # بارگذاری ثابت‌ها
    la   t0, const_9_5
    flw  ft0, 0(t0)         # ft0 = 9/5 = 1.8
    
    la   t0, const_32
    flw  ft1, 0(t0)         # ft1 = 32
    
    # محاسبه C × 9/5
    fmul.s ft2, fa0, ft0    # ft2 = C × 1.8
    
    # افزودن 32
    fadd.s fa0, ft2, ft1    # fa0 = (C × 1.8) + 32
    
    ret

.data
const_9_5: .float 1.8
const_32:  .float 32.0
```

</div>

---

## مقایسه

### ⚖️ دستورات مقایسه

<div dir="ltr">

```assembly
# مقایسه و تنظیم بیت
feq.s rd, fs1, fs2      # rd = (fs1 == fs2) ? 1 : 0
flt.s rd, fs1, fs2      # rd = (fs1 < fs2)  ? 1 : 0
fle.s rd, fs1, fs2      # rd = (fs1 <= fs2) ? 1 : 0

# برای double
feq.d rd, fs1, fs2
flt.d rd, fs1, fs2
fle.d rd, fs1, fs2
```

</div>

### 🎯 مثال: پیدا کردن بیشینه

<div dir="ltr">

```assembly
# یافتن حداکثر دو عدد

find_max:
    # ورودی: fa0, fa1
    # خروجی: fa0 = max(fa0, fa1)
    
    # مقایسه
    flt.s t0, fa0, fa1      # t0 = (fa0 < fa1)
    
    beqz t0, already_max    # اگر fa0 >= fa1
    
    # fa1 بزرگتر است
    fmv.s fa0, fa1
    
already_max:
    ret

# یا استفاده از دستور مستقیم:
find_max_simple:
    fmax.s fa0, fa0, fa1    # fa0 = max(fa0, fa1)
    ret
```

</div>

---

## استثناها و خطاها

### ⚠️ انواع استثناها

```
┌─────────────────────────────────────┐
│  1. Division by Zero (تقسیم بر صفر)│
│  2. Overflow (سرریز)               │
│  3. Underflow (ریزشدگی)            │
│  4. Invalid Operation (عملیات نامعتبر)│
│  5. Inexact (نتیجه دقیق نیست)      │
└─────────────────────────────────────┘
```

### 🔍 رجیستر وضعیت (fcsr)

<div dir="ltr">

```assembly
# خواندن fcsr
frcsr t0                # t0 = fcsr

# نوشتن fcsr
fscsr t0                # fcsr = t0

# خواندن flags
frflags t0              # t0 = exception flags

# پاک کردن flags
fsflags zero            # flags = 0
```

</div>

**ساختار fcsr:**

<div dir="ltr">

```
┌─────────────┬─────────┬────────┐
│  Reserved   │  frm    │ fflags │
│   24 bit    │  3 bit  │ 5 bit  │
└─────────────┴─────────┴────────┘
              7-5        4-0

fflags (Exception Flags):
  bit 0: Inexact (NX)
  bit 1: Underflow (UF)
  bit 2: Overflow (OF)
  bit 3: Divide by Zero (DZ)
  bit 4: Invalid Operation (NV)

frm (Rounding Mode):
  000: RNE (Round to Nearest, ties to Even)
  001: RTZ (Round Towards Zero)
  010: RDN (Round Down)
  011: RUP (Round Up)
  100: RMM (Round to Nearest, ties to Max Magnitude)
```

</div>

---

## مثال‌های عملی

### 1️⃣ محاسبه میانگین

<div dir="ltr">

```assembly
# محاسبه میانگین آرایه

calculate_average:
    # ورودی: a0 = آدرس آرایه, a1 = تعداد
    # خروجی: fa0 = میانگین
    
    addi sp, sp, -16
    sw   ra, 12(sp)
    sw   s0, 8(sp)
    sw   s1, 4(sp)
    sw   s2, 0(sp)
    
    mv   s0, a0             # آرایه
    mv   s1, a1             # تعداد
    li   s2, 0              # شمارنده
    
    # مقداردهی اولیه sum = 0
    fcvt.s.w ft0, zero      # ft0 = 0.0
    
loop_sum:
    bge  s2, s1, calc_avg
    
    # بارگذاری arr[i]
    slli t0, s2, 2
    add  t0, s0, t0
    flw  ft1, 0(t0)
    
    # جمع
    fadd.s ft0, ft0, ft1
    
    addi s2, s2, 1
    j    loop_sum
    
calc_avg:
    # تبدیل تعداد به float
    fcvt.s.w ft1, s1
    
    # تقسیم
    fdiv.s fa0, ft0, ft1
    
    lw   s2, 0(sp)
    lw   s1, 4(sp)
    lw   s0, 8(sp)
    lw   ra, 12(sp)
    addi sp, sp, 16
    ret
```

</div>

---

## 🎓 تمرین‌های پیشنهادی

### تمرین 1: محاسبه فاصله اقلیدسی

<div dir="ltr">

```c
// محاسبه فاصله بین دو نقطه
float distance(float x1, float y1, float x2, float y2) {
    float dx = x2 - x1;
    float dy = y2 - y1;
    return sqrt(dx*dx + dy*dy);
}
```

</div>

---

## 🐛 اشکالات رایج

### ❌ خطای 1: فراموشی تراز حافظه

<div dir="ltr">

```assembly
# ❌ اشتباه
.data
value1: .byte 1
value2: .float 3.14     # ممکن است تراز نباشد!

# ✅ صحیح
.data
.align 2                # تراز 4 بایتی
value1: .byte 1
.space 3                # padding
value2: .float 3.14
```

</div>

### ❌ خطای 2: اختلاط رجیسترها

<div dir="ltr">

```assembly
# ❌ اشتباه
add  a0, fa0, fa1       # fa0 و fa1 رجیسترهای float هستند!

# ✅ صحیح
fadd.s fa0, fa0, fa1    # استفاده از دستور float
```

</div>

---

## 📊 جدول مقایسه

### Single vs Double Precision

<div dir="ltr">

| ویژگی | Single (32-bit) | Double (64-bit) |
|-------|----------------|-----------------|
| **اندازه** | 4 بایت | 8 بایت |
| **دقت** | ~7 رقم | ~16 رقم |
| **محدوده** | ±10³⁸ | ±10³⁰⁸ |
| **سرعت** | سریع‌تر ⚡ | کندتر |
| **حافظه** | کمتر 💾 | بیشتر |
| **کاربرد** | بازی‌ها، گرافیک | علمی، مالی |

</div>

### دستورات اصلی

<div dir="ltr">

| عملیات | Single | Double |
|--------|--------|--------|
| **جمع** | `fadd.s` | `fadd.d` |
| **تفریق** | `fsub.s` | `fsub.d` |
| **ضرب** | `fmul.s` | `fmul.d` |
| **تقسیم** | `fdiv.s` | `fdiv.d` |
| **جذر** | `fsqrt.s` | `fsqrt.d` |
| **مقایسه** | `feq.s, flt.s, fle.s` | `feq.d, flt.d, fle.d` |
| **بارگذاری** | `flw` | `fld` |
| **ذخیره** | `fsw` | `fsd` |

</div>

---

## ❓ سوالات متداول (FAQ)

### Q1: چرا 0.1 + 0.2 ≠ 0.3؟

**A:** به دلیل نمایش دودویی محدود:

<div dir="ltr">

```
0.1₁₀ = 0.000110011001100...₂ (تکرار می‌شود)
0.2₁₀ = 0.001100110011001...₂ (تکرار می‌شود)

با 23 بیت کسر، نمی‌توان دقیقاً نمایش داد!
```

</div>

### Q2: چگونه اعداد را مقایسه کنیم؟

**A:** از یک epsilon استفاده کنید:

<div dir="ltr">

```assembly
# مقایسه با epsilon
fsub.s ft0, fa0, fa1    # diff = a - b
fabs.s ft0, ft0         # |diff|
la   t0, epsilon
flw  ft1, 0(t0)
flt.s t0, ft0, ft1      # |diff| < epsilon?
```

</div>

---

## 🏆 چک‌لیست برنامه‌نویسی Float

### ✅ قبل از کد

- [ ] نوع دقت مشخص است؟ (single/double)
- [ ] محدوده اعداد بررسی شده؟
- [ ] دقت موردنیاز مشخص است؟

### ✅ در حین کد

- [ ] از دستورات صحیح استفاده شده؟ (`.s` یا `.d`)
- [ ] تراز حافظه رعایت شده؟
- [ ] رجیسترهای صحیح استفاده شده؟

### ✅ بررسی خطا

- [ ] تقسیم بر صفر چک می‌شود؟
- [ ] NaN و Infinity بررسی می‌شوند؟
- [ ] flags استثناها خوانده می‌شوند؟

---

<div align="center">

## 🎯 خلاصه نکات کلیدی

<div dir="ltr">

| موضوع | نکته مهم |
|-------|----------|
| **نمایش** | IEEE 754: Sign + Exponent + Fraction |
| **دقت** | Single: 7 رقم, Double: 16 رقم |
| **دستورات** | همیشه `.s` یا `.d` مشخص کنید |
| **خطاها** | fcsr را برای استثناها چک کنید |
| **مقایسه** | از epsilon برای برابری استفاده کنید |
| **بهینه** | از FMA و cache استفاده کنید |

</div>

---





[⬆ بازگشت به بالا](#محاسبات-اعشاری-floating-point-در-risc-v)

---



📧 سوال دارید? Issue باز کنید!

</div>

</div>